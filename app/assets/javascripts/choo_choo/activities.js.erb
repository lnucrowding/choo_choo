$(function () {
  getActivities();
  $('body').click(getActivities);

  function getActivities() {
    // FIXME: Set default_host somewhere else
    $.getScript('<%= activities_url host: "localhost:3000" %>')
  }
});
